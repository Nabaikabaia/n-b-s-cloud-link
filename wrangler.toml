# Cloudflare Pages configuration
name = "nabees-uploader"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"

# Build configuration
[build]
command = "npm run build"

# Environment variables can be set in Cloudflare dashboard
# or via wrangler CLI
